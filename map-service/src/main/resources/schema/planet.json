{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "planet",
  "description": "Schema for checking validity for game map (.gv format) with inline JSON",
  "type": "object",
  "properties": {
    "id": {
      "description": "Mandatory parameter to attache planet properties from gv file Nodes ",
      "type": "number",
      "minimum": 1,
      "exclusiveMinimum": true
    },
    "type" : {
      "description": "Mandatory parameter to change planet properties used in game mechanics",
      "type": "string"
    },
    "units" : {
      "description": "Mandatory parameter to change planet unit capacity properties",
      "type": "integer"
    },
    "owner" : {
      "description": "Parameter to change planet properties used in game mechanics",
      "type": "string"
    },
    "isStartPoint" : {
      "description": "Mandatory parameter to use planet for into start spot choice",
      "type": "boolean"
    }
  },
  "required": [
    "id",
    "type",
    "units",
    "isStartPoint"
  ]
}
